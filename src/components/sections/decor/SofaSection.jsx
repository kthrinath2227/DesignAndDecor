import React from "react";
import { motion } from "framer-motion";

export function SofaSection() {
     return (
    <section id="SofaSection" className="min-h-screen flex items-center justify-center bg-pink-100">
      <h1 className="text-3xl font-bold">Beds Section</h1>
    </section>
  );v
}